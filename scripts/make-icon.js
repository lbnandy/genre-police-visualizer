'use strict';

// Backwards-compatible entry point. Keep icon generation sourced from the SVG
// files so this script can never silently restore an obsolete embedded design.
require('./render-icons.js');
